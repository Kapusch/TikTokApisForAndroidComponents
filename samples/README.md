# Samples

`Kapusch.TikTok.Android.Sample` documents the smallest Android integration surface without committing a client key.

Provide the manifest placeholder locally and two readable JPEG paths before runtime testing. CI validates the package itself because editor handoff requires an approved TikTok application and a physical device.
